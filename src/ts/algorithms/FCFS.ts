import AlgorithmBase from '../AlgorithmBase';

class FCFS extends AlgorithmBase {
    constructor() {
        super();
    }

    run(trackQueue: number[]) {
        return trackQueue;
    }
}

export default FCFS;